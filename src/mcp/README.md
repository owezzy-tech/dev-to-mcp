# MCP boundary

`mcp` will adapt core use cases to server-side Streamable HTTP MCP tools. It owns protocol validation, metadata, and safe error translation; it must not own workflow policy or infrastructure clients.
