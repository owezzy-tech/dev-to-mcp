# WebMCP boundary

`webmcp` is reserved for the browser adapter. It may expose only user-visible, supported-browser capabilities and must delegate privileged work to backend APIs; runtime registration is intentionally deferred.
